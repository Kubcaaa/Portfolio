---
import { projects } from '../data/projects';
import ProjectRow from './ProjectRow.astro';
---

<section id="work" class="px-6 py-10 xs:px-10 lg:w-2/3 lg:px-12 lg:py-14 xl:px-16">
  {projects.map((project) => <ProjectRow project={project} />)}
</section>
